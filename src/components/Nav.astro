<nav class="navbar-2-component" aria-label="Main navigation" data-pagefind-ignore>
  <div class="navbar-container-menu-top">
    <div class="navbar-2-container">
      <a href="/index.html" class="navbar_logo-link w-nav-brand">
        <img
          width="63"
          loading="lazy"
          alt="JPEG XL"
          src="/images/Logo-JPEG_XL-horizontal-square-full_color.svg"
          class="navbar_logo"
        />
      </a>

      <div class="navbar-navigation-actions">
        <!-- Desktop navigation -->
        <div class="navbar-desktop-items">
        <div class="navbar-desktop-item">
          <button
            type="button"
            class="navbar-desktop-trigger"
            aria-controls="navbar-information"
            aria-expanded="false"
          >
            Information
            <svg class="navbar-chevron" aria-hidden="true" viewBox="0 0 24 24" focusable="false">
              <path d="m7 10 5 5 5-5z"></path>
            </svg>
          </button>
          <div id="navbar-information" class="navbar-desktop-dropdown">
            <a href="/media/newsroom.html">Articles &amp; Coverage</a>
            <a href="/resources/software-support.html">Software Support</a>
            <a href="/resources/battle-of-codecs.html">Battle of the Codecs</a>
            <a href="/resources/faqs.html">FAQs</a>
            <a href="/resources/glossary.html">Glossary</a>
          </div>
        </div>

        <div class="navbar-desktop-item">
          <button
            type="button"
            class="navbar-desktop-trigger"
            aria-controls="navbar-explore"
            aria-expanded="false"
          >
            Explore
            <svg class="navbar-chevron" aria-hidden="true" viewBox="0 0 24 24" focusable="false">
              <path d="m7 10 5 5 5-5z"></path>
            </svg>
          </button>
          <div id="navbar-explore" class="navbar-desktop-dropdown">
            <a href="/resources/jpeg-xl-test-page.html">JPEG XL Test Page</a>
            <a href="/resources/distance-vs-effort-visualizer.html">Distance vs. Effort Visualizer</a>
            <a href="/resources/hdr-test-page.html">HDR Test Page</a>
            <a href="/art/">JXL Art Gallery</a>
            <a href="/art/big_gallery.html" class="navbar-desktop-subitem">Main Gallery (SDR)</a>
            <a href="/art/hdr.html" class="navbar-desktop-subitem">HDR Gallery</a>
            <a href="https://jxl-art.lucaversari.it/" target="_blank" rel="noopener noreferrer" class="navbar-desktop-subitem">Create Your Own</a>
            <a href="/resources/external-tools.html">External Tools</a>
          </div>
        </div>

        <div class="navbar-desktop-item">
          <button
            type="button"
            class="navbar-desktop-trigger"
            aria-controls="navbar-about"
            aria-expanded="false"
          >
            About
            <svg class="navbar-chevron" aria-hidden="true" viewBox="0 0 24 24" focusable="false">
              <path d="m7 10 5 5 5-5z"></path>
            </svg>
          </button>
          <div id="navbar-about" class="navbar-desktop-dropdown">
            <a href="/about/history.html">History</a>
            <a href="/about/contributors.html">Contributors</a>
            <a href="/about/photo-credits.html">Photo Credits</a>
            <hr class="navbar-desktop-divider" />
            <div class="navbar-desktop-social">
              <a
                href="https://discord.gg/DqkQgDRTFu"
                target="_blank"
                rel="noopener noreferrer"
                aria-label="JPEG XL on Discord"
              >
                <img src="/images/Icon-social-discord-black.svg" loading="lazy" alt="Discord" />
              </a>
              <a
                href="https://www.reddit.com/r/jpegxl"
                target="_blank"
                rel="noopener noreferrer"
                aria-label="JPEG XL on Reddit"
              >
                <img src="/images/Icon-social-reddit-black.svg" loading="lazy" alt="Reddit" />
              </a>
              <a
                href="https://x.com/jpegxl"
                target="_blank"
                rel="noopener noreferrer"
                aria-label="JPEG XL on X (Twitter)"
              >
                <img src="/images/Icon-social-x-twitter-black.svg" loading="lazy" alt="X (Twitter)" />
              </a>
            </div>
          </div>
        </div>

        <button
          type="button"
          class="navbar-search-trigger"
          data-search-open
          aria-controls="site-search-dialog"
          aria-haspopup="dialog"
          aria-label="Search site"
        >
          <svg aria-hidden="true" viewBox="0 0 24 24" focusable="false">
            <path d="m21 21-4.35-4.35m1.35-5.15a6.5 6.5 0 1 1-13 0 6.5 6.5 0 0 1 13 0Z"></path>
          </svg>
          <span class="sr-only">Search site</span>
        </button>

        <div id="navbar-jxl-decoder-info" class="navbar-jxl-decoder-info" data-jxl-decoder-info hidden>
          <button
            id="navbar-jxl-decoder-trigger"
            type="button"
            aria-controls="navbar-jxl-decoder-message"
            aria-expanded="false"
          >
            <span aria-hidden="true">i</span>
            <span class="sr-only">JPEG XL decoder information</span>
          </button>
          <div id="navbar-jxl-decoder-message" role="tooltip" hidden>
            Your browser doesn&rsquo;t support JXL natively, so this page is using a local WebAssembly decoder. Rendering may be slower than native support.
          </div>
        </div>
        </div>

        <!-- Mobile actions: decoder information on the left, menu on the right. -->
        <div class="navbar-mobile-actions">
          <button
            type="button"
            class="navbar-search-trigger"
            data-search-open
            aria-controls="site-search-dialog"
            aria-haspopup="dialog"
            aria-label="Search site"
          >
            <svg aria-hidden="true" viewBox="0 0 24 24" focusable="false">
              <path d="m21 21-4.35-4.35m1.35-5.15a6.5 6.5 0 1 1-13 0 6.5 6.5 0 0 1 13 0Z"></path>
            </svg>
            <span class="sr-only">Search site</span>
          </button>
          <div class="navbar-jxl-decoder-info" data-jxl-decoder-info hidden>
            <button type="button" aria-controls="navbar-jxl-decoder-message-mobile" aria-expanded="false">
              <span aria-hidden="true">i</span>
              <span class="sr-only">JPEG XL decoder information</span>
            </button>
            <div id="navbar-jxl-decoder-message-mobile" role="tooltip" hidden>
              Your browser doesn&rsquo;t support JXL natively, so this page is using a local WebAssembly decoder. Rendering may be slower than native support.
            </div>
          </div>
          <div class="navbar_menu-items navbar-mobile-toggle">
            <button
              id="navbar-mobile-trigger"
              type="button"
              class="navbar-toggle navbar_link navbar-top w-inline-block button"
              aria-controls="navbar-mobile-menu"
              aria-expanded="false"
            >
              <span class="sr-only">Open navigation menu</span>
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Mobile full-screen dropdown -->
  <div id="navbar-mobile-menu" class="container-navbar-dropdown-content" hidden>
    <div class="container-navbar-content">
      <div class="navbar-close">
        <button id="navbar-mobile-close" type="button" class="navbar-toggle button" aria-label="Close navigation menu">
          <span class="sr-only">Close navigation menu</span>
        </button>
      </div>
      <div class="navbar-dropdown-content">
        <div class="container-column-vertical">
          <div class="navbar-wrapper-text-section span-with_divider-bottom">
            <div class="text-navbar-menu_section">Information</div>
          </div>
          <a href="/media/newsroom.html">Articles &amp; Coverage</a>
          <a href="/resources/software-support.html">Software Support</a>
          <a href="/resources/battle-of-codecs.html">Battle of the Codecs</a>
          <a href="/resources/faqs.html">FAQs</a>
          <a href="/resources/glossary.html">Glossary</a>
        </div>
        <div class="container-column-vertical">
          <div class="navbar-wrapper-text-section span-with_divider-bottom">
            <div class="text-navbar-menu_section">Explore</div>
          </div>
          <a href="/resources/jpeg-xl-test-page.html">JPEG XL Test Page</a>
          <a href="/resources/distance-vs-effort-visualizer.html">Distance vs. Effort Visualizer</a>
          <a href="/resources/hdr-test-page.html">HDR Test Page</a>
          <a href="/art/">JXL Art Gallery</a>
          <a href="/art/big_gallery.html" class="navbar-mobile-subitem">Main Gallery (SDR)</a>
          <a href="/art/hdr.html" class="navbar-mobile-subitem">HDR Gallery</a>
          <a href="https://jxl-art.lucaversari.it/" target="_blank" rel="noopener noreferrer" class="navbar-mobile-subitem">Create Your Own</a>
          <a href="/resources/external-tools.html">External Tools</a>
        </div>
        <div class="container-column-vertical">
          <div class="navbar-wrapper-text-section span-with_divider-bottom">
            <div class="text-navbar-menu_section">About</div>
          </div>
          <a href="/about/history.html">History</a>
          <a href="/about/contributors.html">Contributors</a>
          <a href="/about/photo-credits.html">Photo Credits</a>
          <div>
            <div class="w-layout-grid navbar-social-links">
              <a
                href="https://discord.gg/DqkQgDRTFu"
                target="_blank"
                rel="noopener noreferrer"
                class="social-link w-inline-block"
              >
                <img src="/images/Icon-social-discord-black.svg" loading="lazy" alt="JPEG XL on Discord" class="icon-social" />
              </a>
              <a
                href="https://www.reddit.com/r/jpegxl"
                target="_blank"
                rel="noopener noreferrer"
                class="social-link w-inline-block"
              >
                <img src="/images/Icon-social-reddit-black.svg" loading="lazy" alt="JPEG XL on Reddit" class="icon-social" />
              </a>
              <a href="https://x.com/jpegxl" target="_blank" rel="noopener noreferrer" class="social-link w-inline-block">
                <img src="/images/Icon-social-x-twitter-black.svg" loading="lazy" alt="JPEG XL on X (Twitter)" class="icon-social" />
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>

<div id="site-search-dialog" class="site-search-dialog" role="dialog" aria-modal="true" aria-labelledby="site-search-title" hidden data-pagefind-ignore>
  <div class="site-search-backdrop" data-search-close></div>
  <section class="site-search-panel" aria-describedby="site-search-description">
    <div class="site-search-heading">
      <div>
        <h2 id="site-search-title">Search</h2>
        <p id="site-search-description" class="sr-only">Enter a term to search the JPEG XL community site.</p>
      </div>
      <button type="button" class="site-search-close" data-search-close aria-label="Close search">&times;</button>
    </div>
    <form class="site-search-form" role="search">
      <label class="sr-only" for="site-search-input">Search the site</label>
      <svg aria-hidden="true" viewBox="0 0 24 24" focusable="false">
        <path d="m21 21-4.35-4.35m1.35-5.15a6.5 6.5 0 1 1-13 0 6.5 6.5 0 0 1 13 0Z"></path>
      </svg>
      <input id="site-search-input" type="search" autocomplete="off" placeholder="Search JPEG XL" />
    </form>
    <p class="site-search-status" id="site-search-status" aria-live="polite">Search pages, guides, FAQs, and articles.</p>
    <div class="site-search-results" id="site-search-results"></div>
  </section>
</div>

<script>
  const desktopItems = Array.from(document.querySelectorAll<HTMLElement>('.navbar-desktop-item'));
  const mobileTrigger = document.querySelector<HTMLButtonElement>('#navbar-mobile-trigger');
  const mobileMenu = document.querySelector<HTMLElement>('#navbar-mobile-menu');
  const mobileClose = document.querySelector<HTMLButtonElement>('#navbar-mobile-close');
  const mobileBar = document.querySelector<HTMLElement>('.navbar-container-menu-top');
  const pageWrapper = document.querySelector<HTMLElement>('.page-wrapper');
  const skipNav = document.querySelector<HTMLElement>('.skip-nav');
  const jxlDecoderInfos = Array.from(document.querySelectorAll<HTMLElement>('[data-jxl-decoder-info]'));
  const jxlDecoderTriggers = Array.from(
    document.querySelectorAll<HTMLButtonElement>('[data-jxl-decoder-info] button'),
  );
  const mobileMedia = window.matchMedia('(max-width: 900px)');
  let lockedScrollY = 0;

  function setBackgroundInert(inert: boolean) {
    [pageWrapper, skipNav].forEach((element) => {
      if (!element) return;
      element.toggleAttribute('inert', inert);
    });
  }

  function getMobileMenuFocusable() {
    if (!mobileMenu) return [];
    return Array.from(
      mobileMenu.querySelectorAll<HTMLElement>(
        'a[href], button:not([disabled]), input:not([disabled]), select:not([disabled]), textarea:not([disabled]), [tabindex]:not([tabindex="-1"])',
      ),
    ).filter((element) => !element.hasAttribute('hidden'));
  }

  function setDesktopMenu(item: HTMLElement, open: boolean) {
    const trigger = item.querySelector<HTMLButtonElement>('.navbar-desktop-trigger');
    item.classList.toggle('is-open', open);
    trigger?.setAttribute('aria-expanded', String(open));
  }

  function closeDesktopMenus(except?: HTMLElement) {
    desktopItems.forEach((item) => {
      if (item !== except) setDesktopMenu(item, false);
    });
  }

  function setJxlDecoderMessage(trigger: HTMLButtonElement, open: boolean) {
    const message = trigger.closest<HTMLElement>('[data-jxl-decoder-info]')?.querySelector<HTMLElement>('[role="tooltip"]');
    trigger.setAttribute('aria-expanded', String(open));
    if (message) message.hidden = !open;
  }

  function closeJxlDecoderMessages(except?: HTMLButtonElement) {
    jxlDecoderTriggers.forEach((trigger) => {
      if (trigger !== except) setJxlDecoderMessage(trigger, false);
    });
  }

  function supportsNativeJxl() {
    return new Promise<boolean>((resolve) => {
      // Cache the verdict per tab session: browsers without native support
      // fail the probe in a few ms, but browsers WITH support must download
      // and decode it — re-racing the timeout on every page load is how the
      // button flickers on in Safari over slow connections.
      let cached: string | null = null;
      try {
        cached = window.sessionStorage.getItem('jxl-native');
      } catch {}
      if (cached !== null) return resolve(cached === '1');

      const probe = new Image();
      let settled = false;
      const finish = (supported: boolean, remember: boolean) => {
        if (settled) return;
        settled = true;
        if (remember) {
          try {
            window.sessionStorage.setItem('jxl-native', supported ? '1' : '0');
          } catch {}
        }
        resolve(supported);
      };

      probe.onload = () => finish(true, true);
      probe.onerror = () => finish(false, true);
      // Safari can natively decode JXL files but does not accept the compact
      // hand-crafted data-URL probe used here previously. Keep this detached
      // so JXL.js cannot intercept it, and use a tiny (~0.5 KB) conventional
      // cjxl-encoded file so slow networks cannot blow the deadline the way
      // the previous 46 KB probe could. A timeout is not remembered — it says
      // nothing about the browser, only about the connection.
      probe.src = '/images/jxl-probe.jxl';
      window.setTimeout(() => finish(false, false), 4000);
    });
  }

  async function showJxlDecoderInfoWhenNeeded() {
    if (!document.documentElement.hasAttribute('data-jxl-wasm-fallback') || !jxlDecoderInfos.length) return;
    if (!(await supportsNativeJxl())) jxlDecoderInfos.forEach((info) => (info.hidden = false));
  }

  desktopItems.forEach((item) => {
    const trigger = item.querySelector<HTMLButtonElement>('.navbar-desktop-trigger');
    const firstLink = item.querySelector<HTMLAnchorElement>('.navbar-desktop-dropdown a');
    if (!trigger) return;

    trigger.addEventListener('click', () => {
      const willOpen = trigger.getAttribute('aria-expanded') !== 'true';
      closeDesktopMenus(item);
      setDesktopMenu(item, willOpen);
    });

    trigger.addEventListener('keydown', (event) => {
      if (event.key === 'ArrowDown') {
        event.preventDefault();
        closeDesktopMenus(item);
        setDesktopMenu(item, true);
        firstLink?.focus();
      }
      if (event.key === 'Escape') {
        setDesktopMenu(item, false);
      }
    });

    item.addEventListener('keydown', (event) => {
      if (event.key === 'Escape') {
        setDesktopMenu(item, false);
        trigger.focus();
      }
    });

    item.addEventListener('focusout', () => {
      requestAnimationFrame(() => {
        if (!item.contains(document.activeElement)) setDesktopMenu(item, false);
      });
    });
  });

  jxlDecoderTriggers.forEach((trigger) => {
    trigger.addEventListener('click', () => {
      const willOpen = trigger.getAttribute('aria-expanded') !== 'true';
      closeJxlDecoderMessages(trigger);
      setJxlDecoderMessage(trigger, willOpen);
    });
  });

  function setPageScrollLocked(locked: boolean) {
    const body = document.body;
    if (locked) {
      if (body.classList.contains('mobile-menu-open')) return;
      lockedScrollY = window.scrollY;
      body.style.top = `-${lockedScrollY}px`;
      body.classList.add('mobile-menu-open');
      return;
    }

    if (!body.classList.contains('mobile-menu-open')) return;
    body.classList.remove('mobile-menu-open');
    body.style.top = '';
    window.scrollTo(0, lockedScrollY);
  }

  function setMobileMenu(open: boolean, moveFocus = false) {
    if (!mobileTrigger || !mobileMenu) return;
    if (open && mobileBar) {
      mobileMenu.style.setProperty('--mobile-menu-top', `${mobileBar.getBoundingClientRect().height}px`);
    }
    mobileMenu.hidden = !open;
    mobileTrigger.setAttribute('aria-expanded', String(open));
    setPageScrollLocked(open && mobileMedia.matches);
    setBackgroundInert(open && mobileMedia.matches);
    if (open && moveFocus) mobileClose?.focus();
    if (!open && moveFocus) mobileTrigger.focus();
  }

  mobileTrigger?.addEventListener('click', () => {
    setMobileMenu(mobileTrigger.getAttribute('aria-expanded') !== 'true', true);
  });

  mobileClose?.addEventListener('click', () => setMobileMenu(false, true));

  mobileMenu?.querySelectorAll('a').forEach((link) => {
    link.addEventListener('click', () => setMobileMenu(false));
  });

  document.addEventListener('pointerdown', (event) => {
    if (
      mobileMenu &&
      !mobileMenu.hidden &&
      !mobileMenu.contains(event.target as Node) &&
      !mobileTrigger?.contains(event.target as Node)
    ) {
      setMobileMenu(false);
    }

    if (!desktopItems.some((item) => item.contains(event.target as Node))) closeDesktopMenus();
    if (!jxlDecoderInfos.some((info) => info.contains(event.target as Node))) closeJxlDecoderMessages();
  });

  document.addEventListener('keydown', (event) => {
    if (event.key === 'Tab' && mobileMenu && !mobileMenu.hidden && mobileMedia.matches) {
      const focusable = getMobileMenuFocusable();
      const first = focusable[0];
      const last = focusable.at(-1);
      if (!first || !last) return;

      if (event.shiftKey && document.activeElement === first) {
        event.preventDefault();
        last.focus();
      } else if (!event.shiftKey && document.activeElement === last) {
        event.preventDefault();
        first.focus();
      }
      return;
    }

    if (event.key === 'Escape') {
      closeDesktopMenus();
      closeJxlDecoderMessages();
      if (mobileMenu && !mobileMenu.hidden) setMobileMenu(false, true);
    }
  });

  mobileMedia.addEventListener('change', () => setMobileMenu(false));
  showJxlDecoderInfoWhenNeeded();
</script>
